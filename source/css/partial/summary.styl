.summary {
  padding: 20px;
  cursor: pointer;

  &-tags {
    display: flex;
    flex-flow: row wrap;
    justify-content: flex-start;
    align-items: flex-start;
    margin: 0 -4px;
  }

  &-tag {
    margin: 4px;
  }
}
