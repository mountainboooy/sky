.archive {
  +unstyled-list(){}

  .archive__grid {
    display: grid;
    grid-gap: 1.5rem;

    grid-template-columns: 1fr;

    +responsive-over(lg) {
      grid-template-columns: 1fr 1fr;
    }

    +responsive(xll) {
      grid-template-columns: 1fr 1fr 1fr;
    }
  }
}
