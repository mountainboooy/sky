a.unstyled-link.summary(href=url_for(post.path))
  .summary__thumbnail
    img(src=post.thumbnail)
  .summary__body
    .summary__date
      = date(post.date, 'YYYY.M.D')
    h3.summary__title= post.title
